import { ProductpricesearchPipe } from './productpricesearch.pipe';

describe('ProductpricesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductpricesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
